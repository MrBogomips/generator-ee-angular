language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ee-angular' ]; then cd .. && eval "mv $currentfolder generator-ee-angular" && cd generator-ee-angular; fi

